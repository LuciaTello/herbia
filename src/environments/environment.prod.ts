// Production environment (like application-prod.properties in Spring)
// API_URL will be replaced at build time with the real Render URL
export const environment = {
  production: true,
  apiUrl: 'https://herbia-server.onrender.com/api',
};
